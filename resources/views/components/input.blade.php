<div>
    <label for="{{ $name }}" class="font-syne text-white text-sm font-bold mb-1 inline-block">{{ $label }}</label>
    <input type="{{ $type }}" placeholder="{{ $placeholder }}" class="font-syne text-white p-2 rounded-md border border-[#fffaed2d] focus:outline-0 focus:border-white w-full" name="{{ $name }}" />
</div>
